<p style="color: green"><%= notice %></p>

<%= render @item %>

<div>
  <%= link_to "Edit this item", edit_item_path(@item), class: "btn btn-primary p-1"  %>
  <%= link_to "Back to items", items_path,class: "btn btn-primary p-1"  %>

  <%= button_to "Destroy this item", @item, method: :delete,class: "btn btn-danger p-1"  %>
</div>
